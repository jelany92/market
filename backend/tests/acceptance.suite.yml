suite_namespace: backend\tests\acceptance
actor: AcceptanceTester
modules:
    enabled:
        - WebDriver:
            url: http://admin.faktencheck.local
            browser: chrome
            #port: 9515 // chrome driver standalone only
            window_size: false
            wait: 2
        - Yii2:
            part: [init,fixtures]
